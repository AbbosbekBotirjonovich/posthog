name: posthog_example
description: PostHog SDK example — Windows, Linux, Android, iOS, macOS va Web'da ishlaydi.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  posthog_dart:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
